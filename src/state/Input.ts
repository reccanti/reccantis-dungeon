import { createAction } from "@reduxjs/toolkit";

export const inputUp = createAction("input/up");
export const inputDown = createAction("input/down");
export const inputLeft = createAction("input/left");
export const inputRight = createAction("input/right");

export const inputDebug = createAction("input/debug");
